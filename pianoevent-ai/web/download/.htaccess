# 설치 파일은 브라우저에서 열지 말고 **내려받게** 한다
<FilesMatch "\.(exe|dmg|zip|AppImage)$">
  ForceType application/octet-stream
  Header set Content-Disposition attachment
</FilesMatch>

# 폴더 안을 목록으로 보여 주지 않는다
Options -Indexes
