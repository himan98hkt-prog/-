# 파이썬 의존성 (지시서 부록 A)
music21>=9.1
mido>=1.3

# 2단계 화성 확인 화면 (지시서 10장)
fastapi>=0.110
uvicorn>=0.27

# 4단계 PDF 업로드 (지시서 9장 4단계) — fastapi 의 멀티파트 수신에 필요하다
python-multipart>=0.0.9

# 개발·테스트
pytest>=8.0
httpx2>=0.28          # fastapi TestClient
