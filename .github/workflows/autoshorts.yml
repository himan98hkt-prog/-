name: AutoShorts CI

# 파이썬 파이프라인은 학원 관리노트(JS)와 독립이라 해당 경로가 바뀔 때만 돈다
on:
  push:
    branches: [main]
    paths: ['autoshorts-engine/**', '.github/workflows/autoshorts.yml']
  pull_request:
    paths: ['autoshorts-engine/**', '.github/workflows/autoshorts.yml']
  workflow_dispatch:

concurrency:
  group: autoshorts-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: autoshorts-engine

jobs:
  # pyproject 의 [tool.ruff.lint] 설정은 있었지만 CI 가 돌리지 않아 gate 가
  # 실제로 걸리지 않았다. 린트 결과는 파이썬 버전과 무관하므로 매트릭스에
  # 넣지 않고 한 번만 돈다.
  lint:
    name: 린트
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: ruff 설치
        run: pip install ruff

      - name: ruff check
        run: ruff check .

  test:
    name: 테스트 (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.12']

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # 테스트는 무거운 의존성(yt-dlp·faster-whisper·Gemini SDK) 없이 통과해야 한다.
      # 지연 임포트가 깨지면 여기서 잡힌다.
      - name: 테스트 도구만 설치
        run: pip install pytest

      - name: 단위·통합 테스트
        run: python -m pytest -q

      - name: 패키지 설치 확인
        run: |
          pip install -e . --no-deps
          autoshorts --version
          autoshorts --help > /dev/null
