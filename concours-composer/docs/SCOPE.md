# 범위 결정 기록

## 2026-08-31 — B축(오디오→MIDI 채보 · 연주 시각화 · 릴스 메이커) 제외

SPEC.md 는 두 축(A: 작곡 · B: 시각화·릴스)을 담고 있으나, 원장 결정으로 **B축은 구현하지 않는다**.
사유: 실제로 활용하지 않는 기능이며, 개발 역량을 **콩쿨곡 작곡 품질** 한 곳에 모으는 편이 낫다.

### 제외되는 SPEC 항목
- §6.10 오디오 → MIDI 채보 (Transcription)
- §6.11 연주 시각화 렌더러 (Synthesia 스타일)
- §6.12 릴스 메이커 (ReelStudio) · 브랜드 킷 · 하이라이트 추천 · 캡션 생성
- §11 M6, M7 마일스톤 전체
- 데이터 모델의 `media_assets` / `transcriptions` / `brand_kits` / `reel_*` 테이블
- API 의 `/api/media`, `/api/transcriptions`, `/api/brand-kits`, `/api/reel-templates`, `/api/reels/*`
- 서비스 `worker-render`, 패키지 `render-worker`, `web/lib/visualizer`
- 의존성 torch · piano_transcription_inference · basic-pitch · librosa · playwright chromium

### 유지되는 것
- `media_consent` 와 이름 마스킹 규칙 — 프로그램북·악보 표지의 학생 이름 표기에 그대로 쓴다.
- `NoteEvent` / `NoteEvents` 스키마 — 재생·미리듣기·음악성 분석이 절대 시간 표현을 쓴다.
- SPEC.md 원문은 기록으로 그대로 둔다. 이 문서가 실제 구현 범위의 기준이다.

### 되살릴 때
Stage 1 의 "흥얼거린 멜로디 → 채보 → 모티브" 입력만 채보가 필요하다.
그때는 §6.10 파이프라인 중 전처리·채보·후처리만 되살리면 되고, 릴스는 여전히 불필요하다.

---

## 재조정된 마일스톤

| | 내용 | 상태 |
|---|---|---|
| M0 | 골격 · check-tools | 완료 |
| M1 | 뷰어 + 재생 | 진행 |
| M2 | 코퍼스 분석 · 난이도 · 표절 | 코어 완료 |
| M3a | 모티브 · Plan 공동작곡 | 코어 완료 |
| M3b | 프레이즈 Realize · 비평 루프 | 코어 완료 |
| M4 | 에디터 | 진행 |
| M5 | 해설 · 운지 · 내보내기 · 모의 심사 | 진행 |
| ~~M6~~ | ~~채보~~ | **제외** |
| ~~M7~~ | ~~시각화·릴스~~ | **제외** |
| M8 | 연주회 빌더 · 품질 고도화(골든 회귀·가중치 보정) | 진행 |

---

## 2026-08-31 (2차) — 정밀 분석 후 개선

원장 요청으로 현재 상태를 정밀 분석하고, 실제 결함과 SPEC 대비 공백을 메웠다.

### 고친 결함 6건
| # | 증상 | 원인 |
|---|---|---|
| 1 | 16마디 곡이 Plan 규칙 검사에서 실패 | 클라이맥스를 프레이즈 경계로 반올림하다 60~80% 대역 밖으로 밀렸다 |
| 2 | Plan 을 수정하면 모의 심사가 409 | 곡→요청을 Plan **객체 동일성**으로 찾고 있었다 |
| 3 | 곡이 어느 요청에서 왔는지 몰랐다 | 2번의 근본 원인. `CompositionResult.request_id` 추가 |
| 4 | `edit_plan` 에 쓰지 않는 의존성 파라미터 | 죽은 코드 |
| 5 | `set.add()` 반환값을 조건으로 쓰던 곳 | 동작은 했으나 의도가 드러나지 않았다 |
| 6 | CI 가 Python 테스트를 전혀 돌리지 않았다 | 워크플로가 npm 프로젝트만 검사 |

### 메운 기능 공백
- **검증기 소프트 경고가 비평가에게 전달되지 않았다.** 병행 5·8도가 매 곡 5~10건씩
  남아 있어도 아무도 몰랐다. 이제 경고가 수정 지시로 전환되고, 문턱을 넘은 곡도
  **마무리 다듬기 패스**를 한 번 거친다(점수가 떨어지면 폐기).
  왼손 베이스 선택에도 병행 회피를 넣어 20곡 기준 **110건 → 15건**으로 줄었다.
- **3안 생성**(§7.9 원칙 5) — 같은 잠긴 모티브로 최대 3안, 종합점수 최고안이 기본.
- **연주법 해설·제목**(§6.6) — 프롬프트만 있고 코드가 없었다. 마디 앵커 검증 포함.
- **MIDI 내보내기 + 모티브 미리듣기** — Stage 1 은 "들어본 뒤 고른다" 가 핵심인데
  들을 방법이 없었다.
- **코퍼스 · StyleProfile · RAG**(§6.1 §7.1 §7.8) — 차별점 #2 가 통째로 비어 있었다.
  업로드 → 파싱 → 프로필 → 검색 → Stage 0 주입 → 표절 검사까지 연결했다.
  저작권곡은 통계만 저장하고 **음표열은 보관조차 하지 않는다**.

### 검색 설계에서 발견한 함정
요청에서 만든 검색 target 은 아직 곡이 아니라 대부분의 칸이 비어 있다. 그 상태로
코사인 유사도를 쓰면 방향이 아니라 **크기**가 이겨서, 난이도 2.5 를 요청해도 가장
화려한 곡이 1등으로 올라왔다. 실제 악보에서 뽑은 프로필이면 전체 벡터 코사인을,
요청에서 만든 것이면 **아는 축(난이도·템포·박자·선법)의 거리**만 쓰도록 나눴다.
