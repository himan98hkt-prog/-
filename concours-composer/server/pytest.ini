[pytest]
testpaths = tests
pythonpath = . tests tests/golden
addopts = -q --strict-markers
markers =
    live: 실제 Anthropic API 를 호출한다 (기본 실행에서 제외; ANTHROPIC_API_KEY 필요)
    golden: 골든 20건 회귀 (make golden)
filterwarnings =
    ignore::DeprecationWarning
