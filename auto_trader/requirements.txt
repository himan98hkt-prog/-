# --- 증권사 / HTTP ---
requests>=2.32.0

# --- AI 판단 엔진 ---
anthropic>=1.4.0            # output_config(Structured Outputs) 사용 — 1.x 필요
google-genai>=1.0.0          # 구 google-generativeai(deprecated) 대신 신규 SDK
openai>=1.100.0              # ChatGPT (선택 — 키가 없으면 호출하지 않음)

# --- 데이터 / 지표 ---
pandas>=2.2.0
ta>=0.11.0
# 설치 실패 시: pip install --use-pep517 ta

# --- 대시보드 (로컬 웹 UI) ---
flask>=3.0.0

# --- 스케줄링 ---
apscheduler>=3.10.4

# --- 설정 ---
python-dotenv>=1.0.1
PyYAML>=6.0.2

# --- 테스트 ---
pytest>=8.3.0
pytest-mock>=3.14.0
